name: Build latest (Switch)
# trigger via either push to selected branches or on manual run
on:
  push:
    branches:
      - main
      - master
  workflow_dispatch:

concurrency:
  group: ${{ github.ref }}-switch
  cancel-in-progress: true

jobs:
  build:
    timeout-minutes: 10
    runs-on: ubuntu-latest
    container:
      image: devkitpro/devkita64:latest
    steps:
      - uses: actions/checkout@v5
      - name: Compile Switch build
        run: |
          make switch
          
          
      - uses: ./.github/actions/upload_build
        with:
          SOURCE_FILE: 'NotClassic-switch.nro'
          DEST_NAME: 'NotClassic-switch.nro'
          
      - uses: ./.github/actions/upload_build
        with:
          SOURCE_FILE: 'NotClassic-switch.elf'
          DEST_NAME: 'NotClassic-switch.elf'
          
          
      - uses: ./.github/actions/notify_success
        with:
          DESTINATION_URL: '${{ secrets.NOTIFY_URL }}'
          WORKFLOW_NAME: 'switch'
          

      - uses: ./.github/actions/notify_failure
        if: failure()
        with:
          NOTIFY_MESSAGE: 'Failed to produce Switch build'
          WEBHOOK_URL: '${{ secrets.WEBHOOK_URL }}'
